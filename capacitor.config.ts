import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ngweatherapp.netlify.app',
  appName: 'Angular Weather App',
  webDir: 'dist/ng-weather-app'
};

export default config;